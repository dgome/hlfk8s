apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: hyperledger
  name: orderer1
  labels:
    app: orderer1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orderer1
  template:
    metadata:
      labels:
        app: orderer1
    spec:
      volumes:
        - name: vol1
          persistentVolumeClaim:
            claimName: pvc1
      containers:
        - name: orderer1
          image: hyperledger/fabric-orderer:2.2.1
          workingDir: /opt/gopath/src/github.com/hyperledger/fabric
          env:
            - name: FABRIC_LOGGING_SPEC
              value: "INFO"
            - name: FABRIC_CFG_PATH
              value: "/vol1/configs"
            - name: ORDERER_GENERAL_SYSTEMCHANNEL
              value: "system-channel"
          volumeMounts:
            - mountPath: /vol1
              name: vol1
            - mountPath: /var/hyperledger/orderer/
              name: vol1
              subPath: organizations/ordererOrganizations/orderers/orderers/orderer1/
            - mountPath: /var/hyperledger/production/orderer/
              name: vol1
              subPath: orderers/orderer1/
---
apiVersion: v1
kind: Service
metadata:
  namespace: hyperledger
  name: orderer1
spec:
  type: ClusterIP
  selector:
    app: orderer1
  ports:
    - port: 7050
      targetPort: 7050
